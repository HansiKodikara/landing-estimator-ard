# Core numerics / ML
numpy>=1.24
scikit-learn>=1.3
pandas>=2.0
pyyaml>=6.0
joblib>=1.3

# Flight simulation ("teacher" that the surrogate learns from).
# Optional: if RocketPy is not installed, the data generator automatically
# falls back to the built-in 3-DOF physics simulator (src/lze/sim/fallback.py).
rocketpy>=1.9 ; python_version >= "3.9"
